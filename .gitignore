# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.DS_Store

# Tool caches
.pytest_cache/
.ruff_cache/
.benchmarks/

# Other agents' local config. CLAUDE.md is tracked because it is the shared
# project brief; these are per-tool scratch that shouldn't diverge in git.
.agents/
.codex/
AGENTS.md

# Virtual environments
.venv
data/

# Scratch output. `scripts/build_drm_network.py --output tmp/...` used to land
# .gpkg files here and they got committed; write to the system temp dir instead.
tmp/

# Built DRM network — generated by scripts/build_drm_network.py, ~1-2 GB. Not committed.
assets/drm_network.gpkg
assets/drm_network.gpkg-shm
assets/drm_network.gpkg-wal